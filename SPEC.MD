# 「构建」模块规格说明

- 状态：Draft
- 日期：2026-07-24
- 目标版本：待排期
- 平台：Android（React Native）

## 1. 背景与目标

ShineWriter 现有的“资料”模块同时承担资料维护、导入导出和 AI 生成角色卡/世界书的职责。AI 生成产物会直接回填到资料编辑器，容易让用户误认为它已自动参与当前项目的写作上下文。

本规格新增一个独立的“构建”模块，用在线 LLM 生成可移植的角色卡和世界书文件。构建产物必须先保存到手机，再由用户在“资料”模块中手动导入；只有导入并按既有规则启用后，写作模块才可引用它们。

本功能的目标是：

1. 提供三种明确的 AI 构建模式。
2. 让构建请求充分使用当前在线 LLM 的上下文容量，不与写作上下文或 AI 流水线共享提示词预算。
3. 将“世界书”定义为包含多条可检索条目的完整合集。
4. 保持构建、资料、写作三者的数据边界，避免影响现有写作体系。

## 2. 最终产品决策

| 项目 | 决策 |
| --- | --- |
| 导航名称 | 底部导航使用两个字：`构建`。 |
| 导航顺序 | `项目｜写作｜构建｜资料｜设置`。 |
| LLM 范围 | 首版仅支持在线 OpenAI 兼容配置；本地 llama.cpp / GGUF 为实验能力，不纳入本模块范围。 |
| 产物存储 | 每次确认产物后使用 Android 系统保存窗口写入手机存储中的 JSON 文件。 |
| 资料库关系 | 构建不直接写入角色、世界书、合集、项目或上下文配置表。用户必须到“资料”手动导入。 |
| 写作关系 | 写作模块只读取已导入且被项目启用的资料；不读取构建页面的草稿、预览或文件索引。 |
| 数据库迁移 | 首版不需要新增 Schema 或迁移。 |

## 3. 范围与非目标

### 3.1 本期范围

- 新增“构建”底部 Tab 和构建页面。
- 通过当前活动的在线 LLM 配置生成角色卡或世界书合集。
- 从手机选择角色卡 JSON/PNG 或世界书 JSON 作为派生构建来源。
- 对 LLM 返回结果做格式校验、结构化预览和保存。
- 删除“资料”模块中角色卡、世界书条目的 AI 生成入口。

### 3.2 明确不做

- 不支持本地 llama.cpp / GGUF 的构建请求、上下文配置或真机适配。
- 不从项目、章节、资料库数据库中自动读取内容作为构建来源。
- 不提供“保存并导入资料库”“直接用于项目”“自动启用”等快捷操作。
- 不在构建页维护一套可影响资料库的角色卡/世界书编辑器。
- 不做云同步、构建产物云端历史、后台批量构建或多轮 Agent 工作流。

## 4. 信息架构与用户流程

### 4.1 页面结构

`构建`页是单页分阶段流程，使用现有 `Header`、`Screen`、`Card`、`Field`、`SegmentedControl`、`Button` 和主题系统，不引入新的视觉体系。

页面包含：

1. 模式选择区。
2. 模式对应的需求表单或来源文件选择器。
3. 本次“输出预留”滑块与上下文预算说明。
4. 生成状态区（排队、生成、取消、失败）。
5. 校验后的结构化结果预览。
6. “重新生成”“返回修改”“查看 JSON”“保存到手机”操作。

顶部副标题固定说明：`独立生成文件；保存后请在资料库手动导入。`

### 4.2 主流程

```text
选择构建模式
  → 填写设定 / 选择来源文件
  → 调整输出预留与世界书条目数
  → 上下文与输出容量校验
  → 调用在线 LLM（可取消）
  → JSON 校验与结构化预览
  → Android 系统保存窗口
  → 用户日后在“资料”中手动导入
  → 写作模块沿用既有资料启用规则
```

构建页面不显示当前项目，也不要求已选择项目。

## 5. 三种构建模式

### 5.1 模式一：独立构建

用户先选择目标类型：`角色卡` 或 `世界书`。

#### 独立角色卡

表单包含：角色名称（可选）、题材/时代、角色定位、核心性格/矛盾、叙事氛围、补充需求。至少需要填写一个有效设定字段。

输出为一张 `chara_card_v3` 角色卡。生成结果应涵盖现有角色编辑器支持的核心字段，例如 `name`、`description`、`personality`、`scenario`、`first_mes`、`mes_example`、`tags`、`alternate_greetings` 等。

#### 独立世界书

表单包含：世界书名称（可选）、题材/时代、核心世界观、希望覆盖的类别、叙事用途、补充需求和条目数量。

输出为一个 `lorebook_v3` 世界书合集，而非单条世界书条目。

### 5.2 模式二：基于指定世界书构建角色卡

用户从手机选择一个世界书 JSON 文件。系统使用现有世界书解析逻辑读取和校验来源，展示世界书名称、条目数、预计输入 Token。用户可填写可选补充提示词，例如角色的职业、阵营、冲突或与某个势力的关系。

LLM 只把该文件的已解析内容当作一次性参考快照；不会保存来源文件路径、不会读取资料库 ID，也不会与之后的世界书修改保持联动。

输出为一张符合该世界观的 `chara_card_v3` 角色卡。

### 5.3 模式三：基于角色卡构建世界书

用户从手机选择一个角色卡 JSON 或含角色卡元数据的 PNG 文件。系统使用现有角色卡解析逻辑读取和校验来源，展示角色名称、简介和预计输入 Token。用户可填写可选补充提示词，例如希望扩展的地区、组织、历史秘密或剧情冲突。

输出为围绕该角色扩展的 `lorebook_v3` 世界书合集，条目必须覆盖人物所处的世界、关系或冲突，不得将角色卡字段机械复制为世界书正文。

## 6. 在线 LLM 与上下文预算

### 6.1 配置前置条件

构建只能使用当前活动且可用的 `openai_compatible` LLM 配置。若未配置在线模型、配置不完整或当前选中本地模型，页面显示原因与“前往 LLM 设置”入口，不展示可提交的生成按钮。

`context_window` 是本次在线模型的最大上下文容量 `C`；`max_output_tokens` 是该配置允许的最大输出上限 `M`。它们仍由现有 LLM 设置和上下文自动化配置统一维护。

构建不会使用写作模块的滑动窗口、章节摘要、资料检索预算或 AI 流水线的阶段比例。一次构建请求在其自身提示词中独享 `C` 的可用容量。

### 6.2 输出预留滑块

角色卡和世界书都拥有本次请求专属的“输出预留”滑块：

- 滑块范围：`1%` 至 `15%`，步进 `1%`。
- 默认值：`5%`。
- 滑块同时显示比例和实际 Token，例如 `5% · 1,638 Token`。
- 切换目标类型、模式或重新打开页面时恢复默认 5%；用户可在本次流程内调整。
- 该滑块不是全局设置，不会修改 `llm_config`、写作配置、资料预算或流水线配置。

令 `p` 为滑块比例，`S` 为安全余量，则：

```text
requestedOutput = round(C × p)
S = max(256, min(1024, round(C × 1%)))
outputReserve = min(requestedOutput, M, C - S)
sourceBudget = C - outputReserve - S
```

界面必须同时显示 `C`、`outputReserve` 和 `sourceBudget`。若 `M` 小于滑块期望值，显示“已受当前 LLM 最大输出 Token 限制”；用户可到设置页提高该配置值，但构建永不绕过 `M`。

### 6.3 可生成性校验

在发送请求前，按目标类型执行最低输出容量校验：

| 目标 | 最低输出预留 |
| --- | --- |
| 角色卡 | 512 Token |
| 世界书合集 | `256 × 条目数量` Token，条目数最少为 2。 |

若预留不足，禁用生成按钮，清晰说明需要提高到的最小滑块比例；若所需比例超过 15%，提示用户减少条目数或使用更大上下文的在线模型。不得在模型响应被截断后将不完整 JSON 当作成功产物。

### 6.4 来源内容超预算

来源文件和用户提示词在调用前均需估算 Token。来源超过 `sourceBudget` 时，不得静默截断。用户必须在以下操作中选择其一：

1. 减少世界书条目或选择更小的来源文件。
2. 调整输出预留（仅在仍满足最低输出要求时）。
3. 使用上下文更大的在线模型配置。

构建请求不拼接任何项目、章节、资料库或流水线内容来填满剩余预算。

## 7. 世界书合集规范

### 7.1 设计原则

世界书是供模型按触发词检索的后台百科与故事指南，而非一次性塞进提示词的大段设定。复杂背景应拆成若干独立的信息碎片，使地点、组织、规则和历史在被相关关键词触发时才进入上下文。[TAVO 世界书指南](https://docs.tavoai.dev/cn/guides/lore-book/)

每条条目必须：

- 有明确的主触发词和 1–5 个别称、同义词或关联词；避免只有过于宽泛的词。
- 使用陈述句写出可验证、可复用的客观设定，不写模型指令、寒暄或 Markdown 标题。
- 只表达一个紧密相关的知识主题；复杂设定必须拆条。
- 具备简洁说明，便于用户导入后在资料库中识别和编辑。

### 7.2 条目规模与覆盖

用户在 2–12 条之间选择条目数量，默认 6 条。生成器根据数量组织覆盖面：

- 2–3 条：优先世界铁律、核心地点/势力、当前主冲突。
- 4–6 条：额外覆盖地理、组织、历史事件、关键物品或秘密。
- 7–12 条：允许更细的势力关系、社会习俗、魔法/科技机制、历史分层和剧情钩子。

若提示词明确指定类别，用户要求优先于上述默认覆盖面。若模式三以角色卡为来源，至少有一条条目描述该角色所处的关键关系、组织或冲突，但不把角色卡本身当作世界书合集。

### 7.3 文件格式

生成后必须序列化为当前资料库已支持的格式：

```json
{
  "spec": "lorebook_v3",
  "spec_version": "1.0",
  "data": {
    "name": "世界书名称",
    "entries": [
      {
        "keys": ["主触发词", "别称"],
        "secondary_keys": ["关联词"],
        "content": "客观设定正文。",
        "comment": "条目说明",
        "enabled": true,
        "constant": false,
        "insertion_order": 0
      }
    ]
  }
}
```

默认 `enabled=true`、`constant=false`。仅当用户明确要求某项世界铁律无条件进入上下文时，才允许将该条目的 `constant` 设为 `true`。

世界书结果校验至少包括：合集名称非空、条目数量等于请求数量、每条含非空 `keys` 与 `content`、排序唯一且连续、无重复主触发词。校验不通过时不展示保存操作。

## 8. 角色卡规范

角色卡输出必须封装为当前资料库可导入的格式：

```json
{
  "spec": "chara_card_v3",
  "spec_version": "3.0",
  "data": {
    "name": "角色名",
    "description": "...",
    "personality": "...",
    "scenario": "...",
    "first_mes": "...",
    "mes_example": "...",
    "system_prompt": "...",
    "post_history_instructions": "...",
    "tags": ["..."],
    "alternate_greetings": [],
    "creator": "ShineWriter 构建",
    "character_version": "1.0"
  }
}
```

生成器须校验角色名称、核心文本字段和数组字段类型；`mes_example` 中使用 `{{char}}`、`{{user}}` 标记说话者。校验应复用现有角色卡导入解析器进行回读验证。

## 9. 生成、预览与保存

### 9.1 LLM 请求

新增构建专用服务。推荐场景标识：

- `construction_character_independent`
- `construction_character_from_worldbook`
- `construction_worldbook_independent`
- `construction_worldbook_from_character`

系统提示词负责固定 JSON 协议、输出条目数、来源资料边界和安全规则；用户提示词及来源文件都只能提供内容需求，不能改变输出协议。调用应复用当前在线 LLM 的网络策略、请求调度、取消机制、错误格式化和用量日志。

生成中显示“排队中”或“生成中”，并提供取消按钮。取消、超时、网络失败、模型配置错误和无效 JSON 都不得生成文件或写入资料库。

### 9.2 结果预览

通过格式校验后才进入预览：

- 角色卡展示名称、简介、性格、标签、开场白及“查看 JSON”。
- 世界书展示合集名称、条目数量、每条的触发词、说明和正文摘要及“查看 JSON”。

预览支持“返回修改需求”和“重新生成”。首版不在构建页编辑并回写资料库；需要深度编辑时，用户应先保存文件，再在“资料”中导入和编辑。

### 9.3 手机存储

点击“保存到手机”后，使用现有 `saveDocuments` 的 Android Storage Access Framework 流程，建议文件名：

- 角色卡：`{角色名}-角色卡.json`
- 世界书：`{世界书名称}-世界书.json`

文件由用户在系统保存窗口中选择目录；不申请宽泛存储权限，不假定可写入固定公共目录。用户取消保存时，预览保持“未保存”状态，且不显示保存成功提示。

构建模块不保存完整产物、副本或隐式资料库记录。用户后续应在“资料”页选择“导入角色卡”或“导入世界书”，选取该 JSON 文件并按现有合集/项目规则管理它。

## 10. 与现有模块的改动边界

### 10.1 导航

在 `TabNavigator` 增加 `Build` Tab，标签为“构建”，使用与现有导航一致的两个字标签风格和图标尺寸。现有四个 Tab 的行为不变。

### 10.2 资料模块

从 `ResourceLibrary` 移除：

- 角色卡与世界书条目的“AI 生成”按钮。
- AI 提示词弹窗、生成状态、回填逻辑和相关文案。
- 只为该 UI 服务的导入、状态与测试代码。

资料模块继续保留手工新建、编辑、导入、导出、合集管理和项目启用关系。现有导出的角色卡、世界书文件可以作为构建模式二和三的来源文件。

### 10.3 服务与测试建议

建议新增或重构：

- `src/screens/BuildScreen.tsx`：构建流程 UI。
- `src/services/constructionAiGenerator.ts`：模式请求、提示词、响应解析与结构校验。
- `src/services/constructionFileService.ts`：标准 JSON 序列化和系统保存封装。
- `__tests__/constructionAiGenerator.test.ts`：预算、模式、解析和 JSON 协议测试。
- `__tests__/constructionFileService.test.ts`：文件命名、保存取消和格式测试。
- `__tests__/BuildScreen.test.tsx`：模式切换、前置校验、取消和预览交互。

现有 `resourceAiGenerator` 可仅保留可复用的解析辅助函数，或由新服务取代；资料页不得继续调用其生成能力。

不增加资料库数据库表，不修改现有角色、世界书、合集、项目或上下文配置的数据写入路径。

## 11. 验收标准

### 11.1 功能验收

- 导航中显示“构建”，五个 Tab 的标签均为两个字。
- 在没有当前项目时仍可进入并完成构建。
- 仅在线 LLM 配置可发起构建；本地模型配置被明确拦截。
- 三种模式都可生成格式正确的角色卡或世界书合集。
- 世界书至少包含两条独立条目，并按用户选择的数量完整返回。
- 输出预留默认 5%，可在 1%–15% 内调整，实际值不会超过 `max_output_tokens`。
- 来源超预算、输出预留不足、模型无效 JSON、取消生成和取消保存均不会产出资料库记录。
- 保存出的角色卡 JSON 可由现有资料库角色导入流程成功导入。
- 保存出的世界书 JSON 可由现有资料库世界书导入流程成功导入，并保留所有条目字段。
- 构建产物在未被用户手动导入前，写作上下文中不可见。
- 资料模块不再出现 AI 生成角色卡或世界书的入口。

### 11.2 回归验收

- 原有角色卡/PNG、世界书 JSON、批量导入导出功能保持可用。
- 项目级资料启用关系、上下文配置、AI 流水线和用量统计保持原有行为。
- 在线 LLM 的 HTTPS/局域网 HTTP 安全策略保持不变。
- Jest、TypeScript、ESLint 和现有资源导入/导出测试通过。

## 12. 实施顺序

1. 新增构建预算纯函数、在线配置前置校验及单元测试。
2. 实现角色卡/世界书合集的提示词、解析、序列化和回读校验。
3. 实现文件保存服务与保存取消处理。
4. 实现 `BuildScreen` 及三种模式的表单、状态和预览。
5. 接入底部导航，并从资料模块删除 AI 生成功能。
6. 补齐组件测试、服务测试和 Android E2E：构建 → 保存 → 资料手动导入 → 项目启用 → 写作引用。
7. 更新 README、CHANGELOG、测试说明和必要的用户指引。

## 13. 完成定义

当用户能够使用在线 LLM 独立或基于文件构建角色卡/多条目世界书合集，安全地保存到手机，并必须通过资料页手动导入后才在写作中生效；同时原资料库不再提供 AI 生成入口，且所有验收与回归项通过时，本规格对应的功能视为完成。
