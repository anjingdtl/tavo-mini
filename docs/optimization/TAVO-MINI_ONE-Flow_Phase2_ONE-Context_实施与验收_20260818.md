# TAVO-MINI ONE Flow Phase 2 — ONE Context

**状态：** 尚未开始。必须在 ONE Memory 稳定后进入。

目标：

- Production Context Planner = 1
- Final Budget Decision = 1
- Frozen Context Truth = 1
- Source Adapter 只提供 Candidate，不再做第二套最终预算
- 复用现有 Elastic / Hierarchical Budget；禁止档位固定 Token 上限
- ONE Frozen Context → Deterministic Stage Projection（不是五套 Stage Budget）
- Findings Aggregator 取代后续 Stage 无脑叠加完整检查报告

Before 对照以 Phase 0 Baseline 的 `duplicateContextRatio` / `stageProjectedContextTokens` 为准。
