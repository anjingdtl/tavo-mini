# 原著续写本轮改造审查修复 — 进度交接文档

> 文档状态：WP-A～WP-H 已完成，待提交并推送主分支
> 更新日期：2026-07-30
> 对应方案：`docs/superpowers/specs/本轮改造方案.md`、`docs/superpowers/specs/本轮改造审查修复方案.md`
> 当前分支：`main`

## 0. 发布基线

- 当前源码 `SCHEMA_VERSION = 26`，README 当前版本为 V2.10.7。
- `CHANGELOG.md` 仍处于 `[Unreleased]`，Schema 26 变更列在未发布内容中；当前 git 无 V2.10.7 发布 tag，现有产物目录也没有对应正式 Release APK。
- 结论：**Schema 26 尚未对外发布**。本轮按 v25→v26 历史升级路径修复，不新增补偿迁移。
- 本轮未构建 Release APK；P0 通过前没有执行 Release 构建。

## 1. Work Package 状态与证据

### WP-A：真实 SQLite v25→v26 迁移数据保全 — ✅

- 移除不必要的 `continuation_analysis_runs` 重建，避免子表外键在 RENAME/CREATE/COPY/DROP 期间级联丢失 Canon 数据。
- 保留 legacy style 行并迁移为 `outdated` 版本画像；新增迁移后 live `PRAGMA foreign_key_check` 与子表外键目标检查。
- 补齐迁移故障注入和 live foreign-key 边界测试。
- 证据：`python scripts/real-sqlite-review-regression.py` 输出 `REAL_SQLITE_REVIEW_REGRESSION: PASS`；覆盖率中的 `v25-to-v26.ts` 为 100%；`migrations-v25-v26` 全绿。

### WP-B：备份恢复外键与 active 指针生命周期 — ✅

- 恢复时先清空 settings 的 Canon/Style 指针，完成 parent rows 插入后再二阶段恢复指针。
- manifest 将 `continuation_settings` 调整到 parent 表之后；恢复后执行 `PRAGMA foreign_key_check` 与 Schema validator。
- 对 active Canon/source/boundary、active Style/source fingerprint/boundary/Canon 关联做恢复前校验。
- 证据：真实 SQLite 回归覆盖 active Canon/Style 往返、外键为空、故障回滚；`backupService.test.ts` 的 active 指针成功/不一致场景全绿；`npm run test:coverage` 通过。

### WP-C：Canon/Style/run 原子激活与 source/style 生命周期 — ✅

- Canon、Style、analysis run、settings active pointers、generation invalidation 在同一 SQLite transaction 中更新，并对关键 `rowsAffected` 做原子断言。
- source 激活、boundary 变更、删除和失效路径会清除/失效 Style 与 active pointer；Schema validator 校验复合外键及 active graph。
- 故障注入验证中途失败会回滚，不留下半激活状态。
- 证据：`activateSnapshotAndStyleProfile`、source activation、schema validator、fault injection 测试全绿；真实 SQLite 原子激活故障注入 PASS。

### WP-D：画像 hash 与风格注入 — ✅

- 新增递归稳定 canonicalization/hash，覆盖完整嵌套 profile、metrics、sample refs、overrides、schema/analyzer metadata；数组按语义保持顺序，sample refs 使用稳定排序。
- Context builder 用完整 hash 校验冻结画像，不触发隐式 Style LLM；风格预算按实际 input budget 的 10% 分配，无固定 16K 上限。
- 证据：`styleProfileHash`、style injection/integration、stage budget 测试全绿；超大上下文测试通过。

### WP-E：续写可见章节编号统一 — ✅

- 工作台、边界、source chapters、prompt Planner/Writer、Story Memory、checkpoint、重建、导出路径统一使用 continuation numbering service。
- 保留内部 position 作为数据库排序键；用户可见章节号不再直接使用 `position + 1`。
- 证据：chapter numbering、Phase 3、Story Memory、style integration 测试全绿；Android emulator 可进入续写工作台并显示“新建续写章节”。

### WP-F：故障注入与回归 — ✅

- 补充迁移 orphan/FK target、备份 active pointer mismatch、恢复事务、Canon/Style/run 原子激活故障场景。
- 证据：`npm run verify`：201 passed suites / 1586 passed tests（1 suite、3 tests 按现有配置跳过）；本轮 coverage：201 passed suites / 1591 passed tests；真实 SQLite 脚本 PASS。

### WP-G：Android emulator 回归 — ✅

- debug APK 构建成功并安装到 `emulator-5554`；首屏、续写入口、创建续写项目、续写工作台流程通过。
- 验收产物：`dist/apk/debug/ShineWriter-V2.10.7-debug.apk`；截图/UI dump/logcat 保存在 `test-logs/`。
- 曾由 emulator 暴露 `continuation_analysis_work_items` 复合外键 validator 错配，已修复为 `(run_id, batch_index) → continuation_analysis_batches`，重建后创建项目成功。
- Android 16 KB compatibility dialog 是当前 debug native x86_64 构建提示，不是应用崩溃；过滤后的 app logcat 无 FATAL/SQLite/ReactNativeJS 错误。

### WP-H：门禁、文档与交付 — ✅

- `npm run verify` 通过。
- `npm run test:coverage` 通过：Statements 72.36%、Branches 59.63%、Functions 76.74%、Lines 74.26%。
- 已阅读并按 `docs/RELEASE_APK_BUILD.md` 核对正式构建要求；本轮不构建 Release APK，避免在未对外发布基线和本轮提交前产生正式产物。
- 本文档已更新；提交将按修复主题拆分，且保留用户提供的修复方案文件，不纳入无关清理。

## 2. P0 结论

P0 三项均通过：

1. v25→v26 真实 SQLite Canon 数据保留，故障回滚不残留半迁移结构；
2. 含 active Canon/Style 的备份可恢复，恢复后 `foreign_key_check` 为空；
3. Canon/Style/run 激活及 source/style 生命周期具备真实事务原子性。

因此满足“允许进入 Release 前置验收”的代码门禁条件，但本轮没有生成 Release APK；正式发布仍需按 release guide 执行签名 APK 构建、apksigner、zipalign、aapt、版本和真机验收。

## 3. 剩余风险

- 真实 SQLite 回归脚本使用 Python sqlite3 构造 v25/v26 最小数据库，覆盖真实 SQLite 事务/外键语义；仍建议发版前用受控真机 v25 数据库样本做一次升级演练。
- Android emulator 完成的是 debug 烟测和 UI 回归，未替代正式签名 Release APK 的真机验收；16 KB native compatibility 提示仍需在 Release 构建后按目标设备矩阵复核。
- 真实模型的风格 map/reduce、超大上下文网络耗时未在本机接入真实 provider；单测已覆盖预算、hash、冻结画像和边界行为。
