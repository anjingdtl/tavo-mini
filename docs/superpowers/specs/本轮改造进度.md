# 原著续写本轮改造 — 进度交接文档

> 文档状态：进行中（交接用）
> 更新日期：2026-07-29
> 对应方案：`docs/superpowers/specs/本轮改造方案.md`
> 当前 git 分支：`wp2-style-analysis`

## 0. 一句话现状

WP1（Schema 26 + repository + 编号服务）✅；WP2（风格分析）✅ 已提交 3 个 commit；WP4（章节编号与排序）✅ 代码+DB 单测+Story Memory/Prompt/Episodic 编号闭环，双审阻塞已修，准备提交；WP3、WP5 未开始。

## 1. 关键基线事实（必读，避免重复踩坑）

- **当前 `SCHEMA_VERSION = 26`**（`src/services/migrations/index.ts:27`）。方案文档写的「Schema 21→22」是相对基线的旧描述，**实际是从 25→26**。本仓库此前一直是 25，本轮第一个迁移是 v25-to-v26。
- **工作区当前有大量未提交改动**（WP1 + WP4 的代码都在工作区里，没有 commit）。WP2 的 3 个 commit 在 `wp2-style-analysis` 分支上。
- **不要手改 `src/constants/version.json`**，它由 `prebuild`（`scripts/generate-version-json.js`）生成。
- 备份格式仍是 **v3**，本轮不升级 backup format；新表 `continuation_style_profiles` 重建后 `backup:true`，restoreOrder 510。
- `generate-migration-fixtures.py` 已严重滞后（`CURRENT_SCHEMA=14`），本轮没动它；文件级数据保存验证靠 `__tests__/migrationTestUtils.ts` 的 mock（已补到 v26）。
- 测试命令：`npm run verify`（lint+typecheck+test:ci）、`npm run test:coverage`、`npx jest <文件名>`。

## 2. 各 Work Package 状态

### WP1：Schema 26 与 repository — ✅ 完成并验证
- 新建 `src/services/migrations/v25-to-v26.ts`：重建 `continuation_style_profiles` 为版本表（RENAME 旧表→CREATE 新表→旧数据迁 `outdated`→DROP，仿 v22-to-v23）；重建 `continuation_analysis_runs` 扩展 `CHECK(stage IN ...)` 加 `style_analysis`/`style_validation`（SQLite 不能 ALTER CHECK，必须 RENAME+CREATE+COPY+DROP）；ALTER `continuation_settings ADD active_style_profile_id`。
- `src/services/migrations/index.ts`：bump 到 26，注册迁移。
- `src/data/schema/createCurrentSchema.ts`：inline `active_style_profile_id` + FK；filter 掉 v21 旧 style 表 CREATE；追加 `buildSchema26CreateSqls()`。
- `src/services/migrations/v19-to-v20.ts`：`CHECK(stage IN ...)` 同步加新 stage（保证全新安装一致）。
- `src/services/database/schemaManifest.ts`：两表 columns/indexes 更新。
- `src/services/continuation/canon/types.ts`：`AnalysisStage` 加 `style_analysis|style_validation`。
- 新建 `src/services/continuation/styleProfile/styleProfileRepository.ts`：完整 CRUD + `getInjectableStyleProfile(projectId, fingerprint)`（注入前校验 active/ready/review!=ignored/fingerprint/boundary）+ `setActiveStyleProfileId` + `invalidateStyleProfilesForProject` + `insertStyleProfile`（含可选 `userOverridesJson`，WP2 fix2 加的）+ `updateStyleProfileState/Payload/ReviewStatus` + `saveStyleProfileUserOverrides`。
- 新建 `src/services/continuation/chapterNumbering/continuationChapterNumbering.ts`：`getContinuationChapterNumbering`（只读边界→显示号映射）+ `getNextContinuationChapterPosition`（max+1）+ `renumberContinuationChapterTitles`（边界变化重算自动标题，保护自定义）+ `makeContinuationChapterNumbering`（纯函数）+ `AUTO_TITLE_REGEX`（`/^第\s*(\d+)\s*章$/`）+ `isAutoChapterTitle`。
- 测试：`__tests__/migrations-v25-v26.test.ts`、`__tests__/continuationChapterNumbering.test.ts`（纯函数部分）、更新 `__tests__/migrationMatrix.test.ts`（加 25→26 专项测试 + v26 索引断言）、更新 5 个 `migrations-v*.test.ts` 的 `toBe(25)`→`toBe(26)`、更新 `__tests__/migrationTestUtils.ts`（baseSchemas 补 v20/v21/v26）。
- **验证**：`npx jest migrations-v25-v26 migrationMatrix continuationChapterNumbering` 全绿；backup/schemaValidator 测试全绿；typecheck 干净。

### WP2：风格分析任务 — ✅ 完成（3 个 commit 已提交，已过 spec + 代码质量双审）
分支 `wp2-style-analysis` 上 3 个 commit：
- `77454a2` feat：style analysis 全套（统计/抽样/V2 schema/prompt/service）+ 原子激活
- `73c42cc` fix：activateSnapshot 改为薄 delegate 走统一激活 + 重分析保留 user overrides
- `1422a75` fix：transient-error retry + 取消 parity + 清理

新建文件（在 WP2 commit 里）：
- `src/services/continuation/styleProfile/styleStatistics.ts` — `computeStyleMetrics()`（全量本地统计，CJK/UTF-16 适配）
- `src/services/continuation/styleProfile/styleSampler.ts` — `sampleForStyleAnalysis()`（xmur3+mulberry32 确定性抽样，8 类 sampleKind，只存 ref）
- `src/services/continuation/styleProfile/styleProfileV2Schema.ts` — `OriginalStyleProfileV2` + `validateStyleProfileV2()`
- `src/services/continuation/styleProfile/styleAnalysisPrompt.ts` — prompt/skeleton/repair，`STYLE_ANALYZER_VERSION`
- `src/services/continuation/styleProfile/styleAnalysisService.ts` — `runStyleAnalysis()`（按实际 context_window 动态 map/reduce，一次修复上限，fingerprint 重校验，hash 校验）+ `retryStyleAnalysis()` + `cancelStyleAnalysis()` + `readPriorUserOverrides()`（重分析保留 overrides）+ `callWithTransientRetry()`（复用 canon 的 `isTransientCanonAnalysisError`/`waitForCanonRetry`/`CANON_ANALYSIS_RETRY_POLICY`）+ `styleAnalysisControllers`（取消注册表）
- `src/services/continuation/canon/activateSnapshotAndStyleProfile.ts` — 原子事务（旧 Canon→outdated/新→ready/旧 style→outdated/新→ready/两 active pointer/run 完成/gen run outdated），支持 `allowStyleSkip`
- 测试：`__tests__/styleStatistics.test.ts`、`styleSampler.test.ts`、`styleProfileV2Schema.test.ts`、`styleAnalysis.test.ts`、`activateSnapshotAndStyleProfile.test.ts`、`activateSnapshotDelegate.test.ts`

改动文件：
- `src/services/continuation/canon/canonAnalysisService.ts`：`processAnalysisRunInner()` 成功路径改为 `finalizing(awaiting_review) → style_analysis → style_validation → activateSnapshotAndStyleProfile`；style 失败→Canon 保持 awaiting_review、run=failed(stage=style_analysis)、错误「原著风格分析失败，可单独重试」；`activateSnapshot` 改为薄 delegate；导出 `isTransientCanonAnalysisError`/`waitForCanonRetry` 给 style service 用。

**不变量已守住**：风格分析只经 SourceReader 读 bounded source、样本不越 boundary、不存长原文、激活原子无半成品、Context Builder 不触发 style LLM。

### WP4：章节编号与排序 — ✅ 完成（待提交）
**状态：typecheck 干净；jest 编号/SM/Phase3/episodic 相关全绿；spec 审阻塞项（Planner/Writer 显示号 + episodic 前缀）已修；代码质量审 PASS。**

覆盖面（§11.3）：
- 工作台列表/新建（max+1 + 默认标题 + 防双击）、排序 `position ASC, id ASC`
- 导出 MD/TXT（numbering 每导出只拉一次）
- Context preview / useChapterPipeline 默认指令
- pipelineRunner draft 标题（continuation）
- Story Memory 状态文案 / Toast / 错误 / Renderer 前缀
- `continuationContextBuilder` SM + episodic 共用 getDisplayNumber
- `continuationPromptCompiler` Planner/Writer user 消息用 `displayTargetTitle`（boundary 感知）
- 边界变更 / source 激活后 `renumberContinuationChapterTitles`（best-effort，保护自定义标题）

测试：`continuationChapterNumbering.test.ts` 含 pure + getNext + renumber + episodic prefix；`continuationPhase3Core` 含 Planner/Writer 显示号。

非阻塞遗留：outline `contextBuilder` 未传 getDisplayNumber；`storyMemoryBatchValidator` 仍 position+1；JSON 导出保留内部 title/position。

### WP3：动态预算与生成注入 — ❌ 未开始
详见方案 §7、§8、§9。核心：
- `continuationContextBudget.ts`：加 `ResolvedStageCapacity`/`ContinuationStageBudgets`（§7.1），每阶段按**实际** model context_window 算 inputBudget（不共用 Writer、不取多模型最小窗口），加 `styleTokens`。
- 新建 `styleProfileRenderer.ts`：`renderStyleProfile(profile, level)` compact/standard/detailed（§7.2），Allocator 选最高可容纳层级，不任意截断。
- `continuationContextBuilder.ts`：**删除行 286-314 的裸 SQL 读 style**，改走 `styleProfileRepository.getInjectableStyleProfile`（校验 active/ready/review/fingerprint/boundary/hash）；冻结 `style` 结构到 snapshot；trace 加 `originalStyle` category；不隐式触发 Style LLM（不变量 #3）。
- `generation/types.ts`：`ContinuationContextSnapshot` 加 `style`（§9 结构：profileId/hash/schemaVersion/analyzerVersion/rendererVersion/sourceFingerprint/boundaryCharOffsetExclusive/frozenProfile/userOverrides）；预算加 `styleTokens`。
- `continuationPromptCompiler.ts`：`styleBlock` 读冻结 profile 按阶段渲染；Planner 加规划相关风格（§8.1）；Writer 全量+边界增量+场景变体+参与人物口吻（§8.2）；Checker 加同冻结画像检查契约（§8.3）；Repair 只违规维度（§8.4）。
- `continuationChecker.ts`：确定性检查扩人称/时态/句长段落/对话比例/AI 腔/口吻串位/章末转场/异常连续重合。
- `continuationGenerationRunner.ts`：run 创建冻结每阶段 `ResolvedStageCapacity` 到 snapshot；resume 复用。
- 新建测试：`continuationStageBudgets.test.ts`、`continuationStyleInjection.test.ts`。

**已知背景**（探索结论，供实现参考）：
- 现有 `ContinuationContextBudgetPlan`（`continuationContextBudget.ts:7-18`）是单一扁平对象，**没有 `styleTokens`，没有 per-stage**。`planContinuationContextBudget` 只接 Writer 的 `modelContextLimit`/`maxOutputTokens`。
- 现有 `styleBlock`（`continuationPromptCompiler.ts:144-150`）只在 `compileWriterMessages` 调用一次；Planner/Checker/Repair 都不注入 style。
- trace 没有 `style` category（`continuationContextBuilder.ts:396-463` 九个 category 不含 style）。
- `ContinuationContextSnapshot.contextBudget`（`types.ts:263-269`）存的是单一 inputBudget，不是 per-stage。

### WP5：UI、测试与文档 — ❌ 未开始
详见方案 §10、§13 WP5、§14.5、§15。核心：
- `CanonAnalysisOverviewScreen.tsx`：加「原著写作风格」卡片（6 状态：未分析/分析中/失败/就绪/过期/已忽略）+ 操作（查看详情/单独重试/编辑修正/忽略/恢复/显式跳过激活）。**注意**：该文件 line 52 的 `RUN_STAGE_LABELS` 已在 WP1 补了 style_analysis/style_validation label。
- 新建 `StyleProfileDetailScreen.tsx`：详细画像查看 + 用户修正编辑。
- `ContinuationGenerationConfigScreen.tsx`：加「文风约束：关闭/平衡/严格」SegmentedControl 绑定 `styleLevel`（§10.2）；预设切换展开为子项（当前 `styleLevel` 有字段无 UI 控件，默认 balanced）。
- `ContextPreviewScreen.tsx`：资料分配加「原著风格画像」（级别/token/降级原因）；实际请求预览区分 Planner/Writer（§10.3）。
- E2E：`e2e/maestro/` 加完整链路/失败重试/跳过/边界重算/连续定稿/备份恢复/冷启动归一。
- 文档：`README.md`（Schema→26、续写风格与编号）、`CHANGELOG.md`（新增 `[2.11.0]` 段）、Schema 文档同步。
- 集成测试：`__tests__/continuationStyleIntegration.test.ts`（完整链路、降级 trace、备份恢复编号一致）。

## 3. 已知风险与遗留

- **WP4 非阻塞遗留**：outline `contextBuilder` 未传 getDisplayNumber；`storyMemoryBatchValidator` 仍 position+1；JSON 包导出保留内部 title。
- **真机/真模型回归**：WP2 的 map/reduce 拆分、大窗口单次调用只用 mock+单测覆盖，发版前需真机真模型回归。
- **`activateSnapshot` UI 路径**：`CanonAnalysisOverviewScreen` 的 `handleActivate` 仍调薄 delegate，WP5 应显式传 `styleProfileId`/`allowStyleSkip`。

## 4. 工作区未提交改动清单（全部是 WP1+WP4，需要提交）

已跟踪文件改动（`git status --short --untracked-files=no`）：21 个文件，含 WP1 的 schema/migration/manifest/types/test + WP4 的 repository/screen/exportService/settings/import。
未跟踪新文件：`__tests__/continuationChapterNumbering.test.ts`、`__tests__/migrations-v25-v26.test.ts`、`docs/superpowers/specs/本轮改造方案.md`、`docs/superpowers/specs/本轮改造进度.md`（本文档）、`src/services/continuation/chapterNumbering/continuationChapterNumbering.ts`、`src/services/continuation/styleProfile/styleProfileRepository.ts`、`src/services/migrations/v25-to-v26.ts`。

## 5. 建议的下一步执行顺序

1. **先收尾 WP4**：跑测试、补 DB 函数单测、双审、提交。
2. **再做 WP3**（动态预算+注入）——这是质量核心，且 WP5 的 Context preview 区分 Planner/Writer 依赖 WP3。
3. **然后 WP5**（UI+集成测试+文档）。
4. **全局收尾**：`npm run verify` + `npm run test:coverage`，final review，报告。

建议继续用 **subagent-driven-development** 流程：每个 WP 派 implementer 实现+自测，再 spec-reviewer + code-quality-reviewer 双审，问题回 implementer 修。
