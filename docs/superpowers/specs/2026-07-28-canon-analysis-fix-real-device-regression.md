# 原著分析与续写事实复核 — 真机回归记录

> 日期：2026-07-29
> 设备：`emulator-5554`（ShineWriter V2.10.4 debug）
> 项目：`CanonTestCanonTest` / 《白篱梦》/ deepseek-v4-flash

## 已验证的基础回归

- 近端 30 章分析已验证五类资料均可入库并带原文证据；此前激活快照 `aeee2c1c…` 的快速回归计数为：世界观 4、人物 9、关系 2、剧情 2、经历 4，26 条证据均可回读到其保存的原文范围。
- S1：空响应和字段不匹配会给出具体中文诊断并有限重试；不再把任何空响应笼统报为“模型不支持 JSON”。
- S2：运行中、失败、暂停和终态都使用中文状态；不再在终态持续显示“正在汇总结果”。
- S3：校验器接受已观察到的字段别名；在模型将原句概括为近义表达时，先在原文中定位并保存实际原句，无法定位的资料不会入库。

## 完整分析真机回归（已完成）

- 完整分析改为每 20 章一段、每段“人物与状态”与“世界观与剧情”两次深挖，共 15 段 / 30 个请求组；上下文窗口仍只作为上限，章节正文不再因为固定 6000 字截断。
- 模型保留默认思考能力；不再通过关闭 thinking 换取速度。为其保留输出预算并按章节段分批，最终只接收结构化正文。
- 真机 run `965c8421…` 于 `completed / finalizing / 30/30` 终态完成，无错误码；快照 `2e515975…` 为 `ready`，并已自动写入 `continuation_settings.active_canon_snapshot_id`。
- 覆盖为 `299/299` 章。最终计数：世界观 30、人物 160、人物状态 59、人物关系 64、剧情 54、人物经历 52、知识边界 57、时间线 60。五类核心资料（世界观/人物/关系/剧情/经历）均大于 0。
- 完整性审计：关系、经历、知识、人物状态及剧情人物关联的孤儿人物 ID 均为 0。54 条剧情均有地点、人物 ID、事件，51 条有原文明确时间；60 条时间线均有时间、地点、人物 ID、事件。时间未明的 3 条剧情明确保留空值而非猜测。
- 真机 UI 已显示“100% · 分析完成，已启用为当前原著资料”和“精读原著资料：覆盖 299/299 章”，不再显示“待审核激活”。

## 完整续写真机回归（已完成）

- 在上述已启用快照上，从章节编辑器发起一章“AI 续写”。运行 `ct_d323ed…` 固定绑定快照 `2e515975…`，到 `awaiting_user / awaiting_user` 终态完成，无错误码，生成草稿 3,608 字。
- `llm_usage_logs` 记录规划、写作、事实复核三次调用均为 `success`，模型为 `deepseek-v4-flash`。复核调用实际收到 11,205 输入 token；结果页上下文追踪显示 Canon 快照为 ready，并已注入 3,819 Canon token。
- 复核结果为 blocking 0、error 0、warning 0；草稿保留在待用户确认状态，未自动覆盖章节正文。这验证了完整“规划 → 写作 → Canon 事实复核 → 用户确认”流程，而不把测试草稿误当作用户已采纳内容。

## 续写事实复核

- 续写项目使用独立的“规划 → 写作 → 一致性复核 → 定向修复”链路，不走普通章节流水线。
- 复核与修复提示词现在同时携带世界规则、人物、人物状态、关系、经历、知识边界、剧情线索、时间线和对应证据编号；无角色名的指令也会保留主要人物作为基础资料。
- 成功分析会自动启用快照并默认采纳带证据的条目；用户只需删除或调整不需要的资料。

## 终态验收

- [x] 完整分析到 `completed` / 30/30。
- [x] 快照覆盖 299/299 章，五类资料计数均大于 0。
- [x] 入库资料均经证据定位器处理；无孤儿证据和越过分析边界的证据计入可用快照。
- [x] 在完整快照下完成一次续写与事实复核真机验证；规划、写作、复核在线调用均成功，草稿按设计等待用户确认。
