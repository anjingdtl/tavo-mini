============ 接力提示词（直接 paste 给新 agent 当 User Message）============

你是一个高级 Android + React Native 开发者，接力完成 tavo-mini 项目的 llama.cpp 本地离线模型集成最后一个关键问题。

## 上一个 agent 已经做完的

1. UI 反馈层修复：用户点完 GGUF 文件不再"没反应"，会弹 Modal "导入失败：本地模型引擎初始化失败，请重启 App 后再试。"
2. store 加 preparing 状态 + 90s 兜底 + 中文错误消息
3. isLlamaCppAvailable() 加 NativeModules / __turboModuleProxy / TurboModuleRegistry 三路探测
4. 新增 probeLlamaCppAvailable 异步探测
5. LlamaCppPackage.getModule 加 try-catch + AFTER_ROUTING 日志
6. 4 个回归测试 + 全套 253/253 通过 + lint 0 errors
7. 全部 commit `72ec531` 已 push 到 main 分支

## 真正的根因（你需要解决）

LlamaCpp 这个 TurboModule 没有 codegen spec，所以 RN 0.85 bridgeless 模式下 JS 端通过任何路径（NativeModules.LlamaCpp / global.__turboModuleProxy / TurboModuleRegistry.get）都拿不到这个模块。

logcat 证据：
  LlamaCppPackage.getModule: name='LlamaCpp' (BEFORE ROUTING)
  LlamaCppPackage: instantiating LlamaCppModule
  LlamaCppPackage.getModule: name='LlamaCpp' AFTER ROUTING (result=LlamaCppModule)
  // 后面再没有 importModel 调用 → JS 端没拿到

## 你要做的事（按优先级）

### P0：让 JS 端能调到 LlamaCpp native

首选方案 A — 加 codegen spec：

1. 新建 src/native/specs/NativeLlamaCpp.ts，定义 TurboModuleRegistry.getEnforcing<Spec>('LlamaCpp') 的 TS interface，覆盖现有 LlamaCppModule.ts 暴露的所有方法。
2. 给 android/app/src/main/java/com/shinewriter/llamacpp/LlamaCppModule.kt 加上 @ReactModule(name = "LlamaCpp") 注解。
3. 在 android/app/build.gradle 启用 codegen（检查 react { autolinkLibrariesFromCommand() } 是否存在，必要时加 enableSeparateBuildPerCpuArchitecture 等）。
4. 跑 npx react-native codegen 生成 cpp + Java spec。
5. 重新构建 debug APK（npm run apk:debug）。
6. 装到模拟器验证：global.__turboModuleProxy('LlamaCpp') 在 JS 端返回非 null，且选完 GGUF 文件后 UI 走完 preparing → selecting → copying → hashing → validating → ready 全流程。
7. 跑全套测试 + lint 确认不回归。

如果方案 A 跑不通，用方案 B（退回 legacy module，风险更高）：
1. 去掉 LlamaCppModule 的 implements TurboModule。
2. LlamaCppPackage.getReactModuleInfoProvider 里 ReactModuleInfo.isTurboModule 改成 false。
3. 重新构建、验证 NativeModules.LlamaCpp 不为 undefined。
注意：这会撤销 V2.3.1 → V2.4.0 的 P0-#1 fix，需要在测试机上跑完整路径确认 TurboModuleManager 解析不崩溃。

### P1：真机端到端验收

P0 修完后跑完整 GGUF 导入 → 加载 → 生成链路：
- 测试 GGUF 模型在 test-logs/Qwen2.5-0.5B-Instruct-Q4_K_M.gguf
- 路径：设置 → LLM 设置 → 本地 GGUF → 管理本地模型 → 导入 .gguf 模型 → 选文件
- 验证日志中出现 importModel 真正被调用、UI Modal 切到 copying → hashing → validating → ready
- 在「写作」页用本地模型跑一次续写

### P2：清理 + 文档 + commit

- 临时 debug log 清掉
- 更新 docs/superpowers/plans/2026-07-09-llama-cpp-local-model-progress.md 的第三节状态
- git commit + push 到 main

## 关键环境

- 项目根目录：F:\ClaudeWorkSpace\projects\TAVO-MINI
- ADB：C:\Users\Administrator\AppData\Local\Android\Sdk\platform-tools\adb.exe
- 真机设备 ID：10AEAF31XQ000UQ (Vivo V2405A, Android API 36)
- 当前分支 main（HEAD: 72ec531）
- Windows + Node 24.14.1 + PowerShell 5

## 必读文档

1. F:\ClaudeWorkSpace\projects\TAVO-MINI\docs\superpowers\plans\2026-07-09-llama-cpp-local-model-progress.md（最重要，必读第三节）
2. F:\ClaudeWorkSpace\projects\TAVO-MINI\docs\superpowers\specs\2026-07-09-tavo-mini-llama-cpp-local-model-SPEC.md
3. F:\ClaudeWorkSpace\projects\TAVO-MINI\docs\superpowers\plans\2026-07-09-llama-cpp-local-model.md
4. F:\ClaudeWorkSpace\projects\TAVO-MINI\docs\superpowers\plans\2026-07-09-llama-cpp-bridgeless-codegen-handoff.md（本次接力的详细版）

## 关键代码入口

| 文件 | 看点 |
|---|---|
| android/app/src/main/java/com/shinewriter/llamacpp/LlamaCppModule.kt | @ReactModule 注解 + implements TurboModule（待 P0 修复） |
| android/app/src/main/java/com/shinewriter/llamacpp/LlamaCppPackage.kt | getReactModuleInfoProvider + isTurboModule |
| src/native/LlamaCppModule.ts | findNative() + isLlamaCppAvailable() + probeLlamaCppAvailable() |
| src/store/localModelStore.ts | startImport 状态机 |
| __tests__/localModelImportRegression.test.tsx | 4 个回归测试（不要破坏） |
| node_modules/react-native/Libraries/TurboModule/TurboModuleRegistry.js | TurboModule 解析机制 |

## 验收标准

P0 完成：
1. npm test 通过（253+ 测试）
2. npm run lint 0 errors
3. npm run apk:debug 构建成功
4. 真机/模拟器实测：选完 GGUF 文件后 UI 走完完整状态机
5. 「写作」页能用本地模型续写
6. commit + push 到 main

## 不要做的事

- 不要回滚 commit 72ec531（UI 反馈层修复）
- 不要把 test-logs/ 提交到 git（已在 .gitignore）
- 不要修改 android/app/jni/llama.cpp/ 下的 JNI 源码（已经调通）
- 不要回退到 V2.3.1 release 产物调试（源码已是 V2.4.0）

完成后跟项目 owner 同步进展。

============ end ============