# V1.6.2 Backlog UX 打磨 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 TavoMini 写作管线增加阶段级进度反馈、流水线结果自动弹 Modal、修复备份中心按钮被遮住的问题。

**Architecture:** 三个独立改动。进度条通过扩展 `pipelineRunner.runChapterPipeline` 的 `onStageUpdate` 回调传入阶段信息，ChapterEditor 渲染 `PipelineProgress` 组件。结果弹窗通过新增 `GenerationResultModal` 组件包裹现有 `PipelineResultScreen`，PipelineResultScreen 增加可选 props 支持非路由模式。备份布局通过给 `createRow` 加 elevation/zIndex/backgroundColor 修复。

**Tech Stack:** React Native 0.85.3, TypeScript 5.8, React 19.2, Jest 29, @testing-library/react-native 13.3。

---

## 文件结构

### 新增

- `src/components/PipelineProgress.tsx` — 进度条组件（ActivityIndicator + 阶段名 + 已用时长）
- `src/components/GenerationResultModal.tsx` — Modal 包裹 PipelineResultScreen
- `__tests__/pipelineProgress.test.tsx` — PipelineProgress 组件测试
- `__tests__/generationResultModal.test.tsx` — GenerationResultModal 组件测试
- `__tests__/backupCenterLayout.test.tsx` — 备份中心布局测试

### 修改

- `src/services/pipelineRunner.ts` — `onStageUpdate` 回调参数扩展
- `src/screens/ChapterEditor.tsx` — 接入进度回调 + 渲染 PipelineProgress + 接入 GenerationResultModal
- `src/screens/PipelineResultScreen.tsx` — 增加 `taskId` / `onClose` / `onAdopted` 可选 props
- `src/screens/BackupCenterScreen.tsx` — createRow 样式修复

---

## Task 1: 创建 PipelineProgress 组件

**Files:**
- Create: `src/components/PipelineProgress.tsx`

- [ ] **Step 1: 创建组件文件**

创建 `src/components/PipelineProgress.tsx`：

```tsx
import React, { useEffect, useState } from 'react';
import { ActivityIndicator, StyleSheet, Text, View } from 'react-native';
import { spacing } from './ui';
import { useThemeStore } from '../store/themeStore';
import type { PipelineStageName } from '../types/pipeline';

export interface PipelineProgressProps {
  stage: PipelineStageName | 'idle';
  startedAt: number;
  visible: boolean;
}

const STAGE_LABELS: Record<PipelineStageName | 'idle', string> = {
  idle: '准备中...',
  draft: '草稿中...',
  review: '点评中...',
  factCheck: '事实检查中...',
  proof: '打磨中...',
};

export const PipelineProgress: React.FC<PipelineProgressProps> = ({ stage, startedAt, visible }) => {
  const { theme } = useThemeStore();
  const [elapsed, setElapsed] = useState(0);

  useEffect(() => {
    if (!visible) return;
    setElapsed(0);
    const id = setInterval(() => {
      setElapsed(Math.floor((Date.now() - startedAt) / 1000));
    }, 1000);
    return () => clearInterval(id);
  }, [visible, startedAt]);

  if (!visible) return null;

  return (
    <View style={[styles.container, { backgroundColor: theme.colors.surface, borderBottomColor: theme.colors.border }]}>
      <ActivityIndicator size="small" color={theme.colors.accent} />
      <Text style={[styles.label, { color: theme.colors.textSecondary }]}>
        {STAGE_LABELS[stage] || stage}
      </Text>
      <Text style={[styles.timer, { color: theme.colors.textMuted }]}>
        {elapsed}s
      </Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: spacing.sm,
    paddingHorizontal: spacing.lg,
    paddingVertical: spacing.sm,
    borderBottomWidth: StyleSheet.hairlineWidth,
  },
  label: {
    fontSize: 13,
    fontWeight: '600',
  },
  timer: {
    fontSize: 12,
    fontWeight: '700',
    marginLeft: 'auto',
  },
});
```

- [ ] **Step 2: 跑 lint**

Run: `npx eslint src/components/PipelineProgress.tsx`
Expected: 无 error

- [ ] **Step 3: 提交**

```bash
git add src/components/PipelineProgress.tsx
git commit -m "feat(pipeline): add PipelineProgress component"
```

---

## Task 2: 扩展 pipelineRunner onStageUpdate 回调

**Files:**
- Modify: `src/services/pipelineRunner.ts:125-128`（函数签名）
- Modify: `src/services/pipelineRunner.ts:156,209,220,254,271,306,322,400,493,516,526,549`（所有 onStageUpdate 调用点）

- [ ] **Step 1: 修改 runChapterPipeline 签名**

在 `src/services/pipelineRunner.ts`，将 `runChapterPipeline` 的 `onStageUpdate` 参数类型从 `(status: string) => void` 扩展为同时支持旧 string 和新结构化对象：

改前（line 125-128）：

```ts
export async function runChapterPipeline(
  taskId: string,
  chapter: Chapter,
  onStageUpdate?: (status: string) => void,
): Promise<void> {
```

改后：

```ts
export type StageInfo = {
  stage: PipelineStageName | 'idle';
  label: string;
  startedAt: number;
};

export async function runChapterPipeline(
  taskId: string,
  chapter: Chapter,
  onStageUpdate?: (info: StageInfo | string) => void,
): Promise<void> {
```

- [ ] **Step 2: 替换所有 onStageUpdate 调用点**

将所有 `onStageUpdate?.('...')` 调用替换为结构化对象形式。逐个替换：

1. Line 156: `onStageUpdate?.('正在创作初稿...');` → `onStageUpdate?.({ stage: 'draft', label: '草稿中...', startedAt: Date.now() });`
2. Line 209: `onStageUpdate?.('无审核模式，初稿即为完稿');` → `onStageUpdate?.({ stage: 'idle', label: '无审核模式，初稿即为完稿', startedAt: Date.now() });`
3. Line 220: `onStageUpdate?.('正在审阅/评估草稿...');` → `onStageUpdate?.({ stage: 'review', label: '点评中...', startedAt: Date.now() });`
4. Line 254: `onStageUpdate?.('正在根据审阅/评估终审...');` → `onStageUpdate?.({ stage: 'proof', label: '打磨中...', startedAt: Date.now() });`
5. Line 271: `onStageUpdate?.('正在事实核查草稿...');` → `onStageUpdate?.({ stage: 'factCheck', label: '事实检查中...', startedAt: Date.now() });`
6. Line 306: `onStageUpdate?.('正在根据事实核查终审...');` → `onStageUpdate?.({ stage: 'proof', label: '打磨中...', startedAt: Date.now() });`
7. Line 322: `onStageUpdate?.('正在并行审阅与事实核查...');` → `onStageUpdate?.({ stage: 'review', label: '点评中...', startedAt: Date.now() });`
8. Line 400: `onStageUpdate?.('正在终审校对...');` → `onStageUpdate?.({ stage: 'proof', label: '打磨中...', startedAt: Date.now() });`

同样处理 `resumePipeline` 中的 3 个调用点：

9. Line 493: `onStageUpdate?.('正在审阅/评估草稿（续跑）...');` → `onStageUpdate?.({ stage: 'review', label: '点评中...', startedAt: Date.now() });`
10. Line 516: `onStageUpdate?.('正在终审校对（续跑）...');` → `onStageUpdate?.({ stage: 'proof', label: '打磨中...', startedAt: Date.now() });`
11. Line 526: `onStageUpdate?.('正在事实核查（续跑）...');` → `onStageUpdate?.({ stage: 'factCheck', label: '事实检查中...', startedAt: Date.now() });`
12. Line 549: `onStageUpdate?.('正在终审校对（续跑）...');` → `onStageUpdate?.({ stage: 'proof', label: '打磨中...', startedAt: Date.now() });`

- [ ] **Step 3: 同步修改 runFreeformPipeline 签名**

Line 418: `onStageUpdate?: (status: string) => void` → `onStageUpdate?: (info: StageInfo | string) => void`

Line 439: `resumePipeline` 同理。

- [ ] **Step 4: 跑测试 + lint**

Run: `npx jest --runInBand`
Expected: 全部通过（旧调用点传 `() => {}` 不受影响）

Run: `npx eslint src/services/pipelineRunner.ts`
Expected: 无 error

- [ ] **Step 5: 提交**

```bash
git add src/services/pipelineRunner.ts
git commit -m "feat(pipeline): extend onStageUpdate callback with structured stage info"
```

---

## Task 3: ChapterEditor 接入进度条

**Files:**
- Modify: `src/screens/ChapterEditor.tsx`

- [ ] **Step 1: 新增 import**

在 ChapterEditor.tsx 顶部 import 区域添加：

```ts
import { PipelineProgress } from '../components/PipelineProgress';
import type { StageInfo } from '../services/pipelineRunner';
```

- [ ] **Step 2: 新增 state**

在现有 `const [focusMode, setFocusMode] = useState(false);` 之后添加：

```ts
const [currentStage, setCurrentStage] = useState<PipelineStageName | 'idle'>('idle');
const [progressStartedAt, setProgressStartedAt] = useState(Date.now());
const [progressVisible, setProgressVisible] = useState(false);
const [showResultModal, setShowResultModal] = useState(false);
const [resultTaskId, setResultTaskId] = useState<string | null>(null);
```

- [ ] **Step 3: 修改 executeRunPipeline 中的 onStageUpdate 回调**

找到 `executeRunPipeline` 函数（约 line 219），改前：

```ts
await runChapterPipeline(taskId, chapter, () => {});
```

改后：

```ts
await runChapterPipeline(taskId, chapter, (info) => {
  if (typeof info === 'object') {
    setCurrentStage(info.stage);
    setProgressStartedAt(info.startedAt);
  }
  setProgressVisible(true);
});
```

- [ ] **Step 4: 修改流水线完成后的行为**

找到 `executeRunPipeline` 中流水线完成后的逻辑，改前：

```ts
const store = usePipelineTaskStore.getState();
const finishedTask = store.tasks.find((t) => t.id === taskId);
if (finishedTask?.status === 'completed') {
  // @ts-ignore
  navigation.navigate('PipelineResult', { taskId });
} else if (finishedTask?.status === 'failed') {
  Alert.alert('流水线失败', finishedTask.error || '未知错误');
}
```

改后：

```ts
setProgressVisible(false);
const store = usePipelineTaskStore.getState();
const finishedTask = store.tasks.find((t) => t.id === taskId);
if (finishedTask?.status === 'completed') {
  setResultTaskId(taskId);
  setShowResultModal(true);
} else if (finishedTask?.status === 'failed') {
  Alert.alert('流水线失败', finishedTask.error || '未知错误');
}
```

- [ ] **Step 5: 在 JSX 中渲染 PipelineProgress**

在 `Header` 之后、`{!focusMode && (` 之前，插入：

```tsx
{progressVisible && !focusMode && (
  <PipelineProgress
    stage={currentStage}
    startedAt={progressStartedAt}
    visible={progressVisible}
  />
)}
```

- [ ] **Step 6: 跑测试 + lint**

Run: `npx jest --runInBand`
Expected: 全部通过

Run: `npx eslint src/screens/ChapterEditor.tsx`
Expected: 无 error

- [ ] **Step 7: 提交**

```bash
git add src/screens/ChapterEditor.tsx
git commit -m "feat(chapter-editor): integrate PipelineProgress with stage callbacks"
```

---

## Task 4: PipelineResultScreen 增加可选 props 支持非路由模式

**Files:**
- Modify: `src/screens/PipelineResultScreen.tsx`

- [ ] **Step 1: 修改组件签名与内部逻辑**

改前（line 45-60）：

```tsx
export const PipelineResultScreen: React.FC = () => {
  const { theme } = useThemeStore();
  const navigation = useNavigation();
  const route = useRoute<ResultRouteProp>();
  const { tasks, resolveTask } = usePipelineTaskStore();
  const [expanded, setExpanded] = useState<Set<string>>(new Set());

  const task = tasks.find((t) => t.id === route.params.taskId);
  if (!task) {
    return (
      <Screen>
        <Header title="流水线结果" action={<Button label="返回" variant="ghost" onPress={() => navigation.goBack()} />} />
        <Text style={{ padding: spacing.lg, color: theme.colors.textSecondary }}>任务不存在或已被清除。</Text>
      </Screen>
    );
  }
```

改后：

```tsx
export interface PipelineResultScreenProps {
  taskId?: string;
  onClose?: () => void;
  onAdopted?: (text: string) => void;
}

export const PipelineResultScreen: React.FC<PipelineResultScreenProps> = ({ taskId: propTaskId, onClose, onAdopted }) => {
  const { theme } = useThemeStore();
  const navigation = useNavigation();
  let routeTaskId: string | undefined;
  try {
    const route = useRoute<ResultRouteProp>();
    routeTaskId = route.params?.taskId;
  } catch {
    // Not inside a navigation route (Modal mode)
  }
  const taskId = propTaskId ?? routeTaskId;
  const { tasks, resolveTask } = usePipelineTaskStore();
  const [expanded, setExpanded] = useState<Set<string>>(new Set());

  const handleClose = onClose ?? (() => navigation.goBack());

  const task = tasks.find((t) => t.id === taskId);
  if (!task) {
    return (
      <Screen>
        <Header title="流水线结果" action={<Button label="返回" variant="ghost" onPress={handleClose} />} />
        <Text style={{ padding: spacing.lg, color: theme.colors.textSecondary }}>任务不存在或已被清除。</Text>
      </Screen>
    );
  }
```

- [ ] **Step 2: 替换 navigation.goBack 为 handleClose**

在 `handleAccept` 和 `handleReject` 中，将 `navigation.goBack()` 替换为 `handleClose()`：

改前（line 89-90）：

```ts
      Alert.alert('已采纳', '流水线正文已覆盖到章节并保存。');
      navigation.goBack();
```

改后：

```ts
      Alert.alert('已采纳', '流水线正文已覆盖到章节并保存。');
      onAdopted?.(task.finalText);
      handleClose();
```

改前（line 97-99）：

```ts
  const handleReject = () => {
    resolveTask(task.id, 'reject');
    navigation.goBack();
  };
```

改后：

```ts
  const handleReject = () => {
    resolveTask(task.id, 'reject');
    handleClose();
  };
```

同时将 Header 中的 `navigation.goBack()` 也改为 `handleClose`（line 138）：

改前：`<Button label="返回" variant="ghost" onPress={() => navigation.goBack()} />`
改后：`<Button label="返回" variant="ghost" onPress={handleClose} />`

- [ ] **Step 3: 跑测试 + lint**

Run: `npx jest --runInBand`
Expected: 全部通过

Run: `npx eslint src/screens/PipelineResultScreen.tsx`
Expected: 无 error

- [ ] **Step 4: 提交**

```bash
git add src/screens/PipelineResultScreen.tsx
git commit -m "feat(pipeline-result): add optional props for Modal mode"
```

---

## Task 5: 创建 GenerationResultModal 组件

**Files:**
- Create: `src/components/GenerationResultModal.tsx`

- [ ] **Step 1: 创建组件文件**

创建 `src/components/GenerationResultModal.tsx`：

```tsx
import React from 'react';
import { Modal, StyleSheet, View } from 'react-native';
import { PipelineResultScreen } from '../screens/PipelineResultScreen';

export interface GenerationResultModalProps {
  visible: boolean;
  taskId: string | null;
  onClosed: () => void;
  onAdopted?: (text: string) => void;
}

export const GenerationResultModal: React.FC<GenerationResultModalProps> = ({
  visible,
  taskId,
  onClosed,
  onAdopted,
}) => {
  if (!taskId) return null;

  return (
    <Modal
      visible={visible}
      animationType="slide"
      presentationStyle="overFullScreen"
      onRequestClose={onClosed}
    >
      <View style={styles.backdrop}>
        <View style={styles.container}>
          <PipelineResultScreen
            taskId={taskId}
            onClose={onClosed}
            onAdopted={onAdopted}
          />
        </View>
      </View>
    </Modal>
  );
};

const styles = StyleSheet.create({
  backdrop: {
    flex: 1,
    backgroundColor: 'rgba(0,0,0,0.6)',
  },
  container: {
    flex: 1,
  },
});
```

- [ ] **Step 2: 跑 lint**

Run: `npx eslint src/components/GenerationResultModal.tsx`
Expected: 无 error

- [ ] **Step 3: 提交**

```bash
git add src/components/GenerationResultModal.tsx
git commit -m "feat(pipeline): add GenerationResultModal component"
```

---

## Task 6: ChapterEditor 接入 GenerationResultModal

**Files:**
- Modify: `src/screens/ChapterEditor.tsx`

- [ ] **Step 1: 新增 import**

在 ChapterEditor.tsx 顶部 import 区域添加：

```ts
import { GenerationResultModal } from '../components/GenerationResultModal';
```

- [ ] **Step 2: 在 JSX 末尾渲染 Modal**

在 `ChapterEditor` 的 return JSX 中，`</Screen>` 之前，添加：

```tsx
<GenerationResultModal
  visible={showResultModal}
  taskId={resultTaskId}
  onClosed={() => setShowResultModal(false)}
  onAdopted={async () => {
    setShowResultModal(false);
    await loadChapter();
  }}
/>
```

- [ ] **Step 3: 跑测试 + lint**

Run: `npx jest --runInBand`
Expected: 全部通过

Run: `npx eslint src/screens/ChapterEditor.tsx`
Expected: 无 error

- [ ] **Step 4: 提交**

```bash
git add src/screens/ChapterEditor.tsx
git commit -m "feat(chapter-editor): integrate GenerationResultModal for pipeline results"
```

---

## Task 7: 修复备份中心 createRow 布局

**Files:**
- Modify: `src/screens/BackupCenterScreen.tsx:215-220`（styles.createRow）

- [ ] **Step 1: 修改 createRow 样式**

改前：

```ts
  createRow: {
    paddingHorizontal: spacing.lg,
    paddingTop: spacing.md,
    paddingBottom: spacing.sm,
  },
```

改后：

```ts
  createRow: {
    paddingHorizontal: spacing.lg,
    paddingTop: spacing.md,
    paddingBottom: spacing.md,
    borderBottomWidth: StyleSheet.hairlineWidth,
    elevation: 2,
    zIndex: 2,
  },
```

注意：`backgroundColor` 和 `borderBottomColor` 需要用 theme 色值，但 StyleSheet.create 不支持动态值。改为在 JSX 上用 style 透传。

- [ ] **Step 2: 在 JSX 中给 createRow 加动态样式**

找到 `<View style={styles.createRow}>`（约 line 188），改后：

```tsx
<View style={[styles.createRow, { backgroundColor: theme.colors.surface, borderBottomColor: theme.colors.border }]}>
```

- [ ] **Step 3: 修改 FlatList 的 contentContainerStyle**

改前：

```ts
  list: {
    padding: spacing.lg,
    paddingBottom: 96,
  },
```

改后：

```ts
  list: {
    padding: spacing.lg,
    paddingTop: spacing.md,
    paddingBottom: 96,
  },
```

- [ ] **Step 4: 跑测试 + lint**

Run: `npx jest --runInBand`
Expected: 全部通过

Run: `npx eslint src/screens/BackupCenterScreen.tsx`
Expected: 无 error

- [ ] **Step 5: 提交**

```bash
git add src/screens/BackupCenterScreen.tsx
git commit -m "fix(backup-center): fix createRow overlap with FlatList via elevation and zIndex"
```

---

## Task 8: 补充组件测试

**Files:**
- Create: `__tests__/pipelineProgress.test.tsx`
- Create: `__tests__/generationResultModal.test.tsx`
- Create: `__tests__/backupCenterLayout.test.tsx`

- [ ] **Step 1: 创建 PipelineProgress 测试**

创建 `__tests__/pipelineProgress.test.tsx`：

```tsx
import React from 'react';
import { render, act } from '@testing-library/react-native';

jest.mock('../src/store/themeStore', () => ({
  useThemeStore: () => ({
    theme: {
      colors: {
        accent: '#6366f1',
        textSecondary: '#666',
        textMuted: '#999',
        surface: '#fff',
        border: '#ddd',
      },
    },
  }),
}));

import { PipelineProgress } from '../src/components/PipelineProgress';

describe('PipelineProgress', () => {
  beforeEach(() => jest.useFakeTimers());

  afterEach(() => jest.useRealTimers());

  it('renders ActivityIndicator and stage label when visible', () => {
    const { getByText } = render(
      <PipelineProgress stage="draft" startedAt={Date.now()} visible={true} />,
    );
    expect(getByText('草稿中...')).toBeTruthy();
    expect(getByText('0s')).toBeTruthy();
  });

  it('updates elapsed time after 1.5 seconds', () => {
    const { getByText } = render(
      <PipelineProgress stage="review" startedAt={Date.now()} visible={true} />,
    );
    act(() => { jest.advanceTimersByTime(1500); });
    expect(getByText('1s')).toBeTruthy();
  });

  it('renders nothing when visible is false', () => {
    const { queryByText } = render(
      <PipelineProgress stage="draft" startedAt={Date.now()} visible={false} />,
    );
    expect(queryByText('草稿中...')).toBeNull();
  });

  it('updates label when stage changes', () => {
    const { getByText, rerender } = render(
      <PipelineProgress stage="draft" startedAt={Date.now()} visible={true} />,
    );
    expect(getByText('草稿中...')).toBeTruthy();
    rerender(
      <PipelineProgress stage="proof" startedAt={Date.now()} visible={true} />,
    );
    expect(getByText('打磨中...')).toBeTruthy();
  });
});
```

- [ ] **Step 2: 创建 GenerationResultModal 测试**

创建 `__tests__/generationResultModal.test.tsx`：

```tsx
import React from 'react';
import { render } from '@testing-library/react-native';

jest.mock('../src/store/themeStore', () => ({
  useThemeStore: () => ({
    theme: {
      colors: {
        accent: '#6366f1',
        textSecondary: '#666',
        textMuted: '#999',
        textPrimary: '#111',
        surface: '#fff',
        border: '#ddd',
        card: '#f5f5f5',
        danger: '#ef4444',
      },
    },
  }),
}));

jest.mock('../src/store/pipelineTaskStore', () => ({
  usePipelineTaskStore: {
    getState: () => ({ tasks: [], resolveTask: jest.fn() }),
  },
}));

jest.mock('@react-navigation/native', () => ({
  useNavigation: () => ({ goBack: jest.fn(), navigate: jest.fn() }),
  useRoute: () => ({ params: {} }),
}));

import { GenerationResultModal } from '../src/components/GenerationResultModal';

describe('GenerationResultModal', () => {
  it('renders nothing when taskId is null', () => {
    const { queryByText } = render(
      <GenerationResultModal visible={true} taskId={null} onClosed={jest.fn()} />,
    );
    expect(queryByText('流水线结果')).toBeNull();
  });

  it('renders Modal content when visible and taskId provided', () => {
    const { getByText } = render(
      <GenerationResultModal visible={true} taskId="task-1" onClosed={jest.fn()} />,
    );
    expect(getByText('流水线结果')).toBeTruthy();
  });
});
```

- [ ] **Step 3: 创建备份中心布局测试**

创建 `__tests__/backupCenterLayout.test.tsx`：

```tsx
import React from 'react';
import { render } from '@testing-library/react-native';

jest.mock('../src/store/themeStore', () => ({
  useThemeStore: () => ({
    theme: {
      colors: {
        accent: '#6366f1',
        textSecondary: '#666',
        textMuted: '#999',
        textPrimary: '#111',
        surface: '#fff',
        border: '#ddd',
        card: '#f5f5f5',
        danger: '#ef4444',
      },
    },
  }),
}));

jest.mock('../src/services/backupService', () => ({
  listBackups: jest.fn(async () => []),
  createManualBackup: jest.fn(async () => {}),
  restoreFromBackup: jest.fn(async () => {}),
  deleteBackup: jest.fn(async () => {}),
  createPreRestoreBackup: jest.fn(async () => {}),
}));

jest.mock('../src/services/database', () => ({
  openDatabase: jest.fn(async () => ({})),
}));

jest.mock('../src/services/migrations', () => ({
  SCHEMA_VERSION: 8,
}));

jest.mock('@react-navigation/native', () => ({
  useNavigation: () => ({ goBack: jest.fn() }),
}));

import { BackupCenterScreen } from '../src/screens/BackupCenterScreen';

describe('BackupCenterScreen layout', () => {
  it('renders create backup button', async () => {
    const { findByText } = render(<BackupCenterScreen />);
    expect(await findByText('创建备份')).toBeTruthy();
  });
});
```

- [ ] **Step 4: 跑全量测试 + lint**

Run: `npx jest --runInBand`
Expected: 全部通过

Run: `npx eslint .`
Expected: 无 error

- [ ] **Step 5: 提交**

```bash
git add __tests__/pipelineProgress.test.tsx __tests__/generationResultModal.test.tsx __tests__/backupCenterLayout.test.tsx
git commit -m "test: add component tests for PipelineProgress, GenerationResultModal, and BackupCenter"
```

---

## Task 9: 全量验证 + 版本号 + progress.md

**Files:**
- Modify: `package.json`（version）
- Modify: `src/constants/version.json`（自动生成）
- Modify: `progress.md`

- [ ] **Step 1: 跑全量测试 + lint**

Run: `npx jest --runInBand`
Expected: 全部通过

Run: `npx eslint .`
Expected: 0 errors

- [ ] **Step 2: 更新版本号到 1.6.2**

修改 `package.json` 第 3 行：`"version": "1.6.1"` → `"version": "1.6.2"`

Run: `npm run prebuild`
Expected: version.json 更新为 V1.6.2

- [ ] **Step 3: 提交版本号**

```bash
git add package.json src/constants/version.json
git commit -m "chore: bump version to 1.6.2"
```

- [ ] **Step 4: 更新 progress.md**

在 progress.md 的 V1.6.1 小节之后追加 V1.6.2 小节，简述本轮 3 项改动，引用 spec 文件路径。

- [ ] **Step 5: 提交 progress.md**

```bash
git add progress.md
git commit -m "docs: record v1.6.2 changes in progress.md"
```

---

## 自审

- [x] **Spec 覆盖**：spec 第 5 节（进度条）由 Task 1 + Task 2 + Task 3 实现；spec 第 6 节（结果 Modal）由 Task 4 + Task 5 + Task 6 实现；spec 第 7 节（备份布局）由 Task 7 实现；spec 第 10 节（测试）由 Task 8 实现。
- [x] **占位符扫描**：所有步骤都给完整代码或精确命令，无 TBD/TODO。
- [x] **类型一致性**：`StageInfo` 在 Task 2 定义并 export，Task 3 import 使用；`PipelineResultScreenProps` 在 Task 4 定义，Task 5 使用；`GenerationResultModalProps` 在 Task 5 定义，Task 6 使用。
- [x] **向后兼容**：`onStageUpdate` 扩展为 `StageInfo | string`，旧调用点传 `() => {}` 不受影响；`PipelineResultScreen` 新增 props 均可选，现有路由模式不受影响。
