/**
 * Captured from the Final-Seal Standard run for task
 * `pt_mt1qckad_250` (chapter 265). The model identified a hard violation in
 * natural language but omitted the structured findings required by the QA
 * contract.
 */
export const qaContentOnlyFailureFixture = {
  verdict: 'needs_revision',
  content:
    '发现一个阻塞性问题：水漫上第一步后才完成交付，不符合“before water reaches the first step”的硬性要求。',
};
