/**
 * LEGACY shim (Kernel Final Closure §8.2). Continuation stage capacity
 * planning now lives in the unified Writing scenario layer:
 *   src/services/writing/scenario/continuationStageCapacity.ts
 * Only legacy runners / tests may import this path.
 */
export * from '../../writing/scenario/continuationStageCapacity';
